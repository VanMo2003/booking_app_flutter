import 'package:flutter/material.dart';

class HotelierScreen extends StatelessWidget {
  const HotelierScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
