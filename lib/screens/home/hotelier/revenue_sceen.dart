import 'package:flutter/material.dart';

class RevenueSceen extends StatelessWidget {
  const RevenueSceen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Revenue"),
    );
  }
}
